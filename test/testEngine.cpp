#include "engine.h"
#include "variation.h"
#include "catch.hpp"

#include <iostream>
#include <vector>

TEST_CASE("Test Engine")
{
 /*   Chessboard cb;
    cb.importFen("2Q5/1k1Q4/8/6p1/1r6/6PP/2K5/8 b - - 0 68");
    
    Variations v;
    cb.findVariations(v);

    for (Variation v : v)
    {
        //std::cout<<v.movement_.str()<<std::endl;
        cb.makeMove(v.movement_);

        Variations v2;
        cb.findVariations(v2);
        for(Variation v2 : v2)
        {
            cb.makeMove(v2.movement_);
            //cb.undoMove();
        }


        //cb.undoMove();
    }
*/
}